{
  "name": "hermosa-floors",
  "version": "1.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "npm-run-all --parallel build:*",
    "build:shopify": "shopify app build",
    "build:resources": "node esbuild.config.mjs",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@z-elements/reviews-stars": "^1.0.2"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "mitch",
  "private": true,
  "workspaces": [
    "extensions/*"
  ],
  "devDependencies": {
    "esbuild": "0.23.0",
    "npm-run-all": "4.1.5"
  }
}
